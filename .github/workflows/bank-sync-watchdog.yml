name: Bank Sync Watchdog

on:
  # 🔥 核心：监听 "Sniper Elite" 是否跑完
  workflow_run:
    workflows: ["Sniper Elite (Fetch Target Tweets)"]
    types:
      - completed
  
  # 手动触发按钮
  workflow_dispatch:

jobs:
  sync-job:
    # 只有当 Sniper 成功跑完 (success) 才进行同步
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      # 1. 下载 X-Kit (此时它已经包含了 Sniper 刚刚提交的最新数据)
      - name: Checkout Source (X-Kit)
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_TOKEN }}
          path: source_code

      # 2. 下载 Central Bank (准备写入数据)
      - name: Checkout Central Bank
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.CENTRAL_BANK_REPO }}
          token: ${{ secrets.MY_PAT }}
          path: central_bank

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      # 3. 安装依赖 (为了运行同步脚本)
      - name: Install dependencies
        working-directory: ./source_code
        run: bun install

      # 4. 运行同步脚本
      # 这个脚本会自动对比 source_code/tweets 和 central_bank/twitter
      # 只搬运最新的文件
      - name: Run Sync Script
        working-directory: ./source_code
        run: bun run scripts/sync-to-bank.ts

      # 5. 推送到中央银行
      - name: Push to Central Bank
        working-directory: ./central_bank
        run: |
          git config --global user.name 'Watchdog Bot'
          git config --global user.email 'bot@watchdog.alpha'
          
          git add .
          
          # 如果有新文件就提交，没有就跳过
          git commit -m "Watchdog: synced from x-kit $(date +%H:%M)" || echo "No new data to sync"
          
          git push
